# 蓝牙电池快速连接

## 当前支持路径

- 小米/Android Chrome：支持网页 BLE 连接，首页会按 `4803012MNBV230224842` 筛选设备。
- iPhone Safari 或 Edge iOS：可以添加到手机桌面作为 PWA，但网页层不能直接调用 BLE GATT；需要原生伴侣 App。

## 使用步骤

1. 在小米手机 Chrome 中打开工作台的 HTTPS 地址。
2. 打开电池蓝牙，让设备名可见。
3. 首页点击“快速连接电池”。
4. 系统选择器中选择 `4803012MNBV230224842`；网页安全限制下仍需要这一步确认，不能静默自动连接。
5. 查看 GATT 服务、特征、通知和原始十六进制日志。
6. 如果服务为空，把真实 Service UUID 填入输入框后重新连接。

首版只做连接、服务发现、通知订阅和 READ；未知协议前不向 BMS 写入命令。

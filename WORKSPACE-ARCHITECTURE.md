# 工作空间架构

## 关系

```text
个人工作台
└── 工作空间 / 项目
    ├── 项目知识库
    ├── 项目目标
    ├── 项目学习记录
    ├── 项目复盘与历史分析
    └── 项目 AI 上下文
```

昵称、个人偏好和 API Key 等属于全局或本机配置；考试科目、招聘数据、岗位资料、项目学习记录和项目复盘都带有 `workspace_id`，不会混入其他空间。

## 当前空间

- `personal`：长期个人工作台。
- `sinopec-2027`：第一个项目空间，包含中国石化 2027 届备考知识库和备考记录。
- 首次建立工作空间机制时，用户确认历史记录全部属于中石化备考，因此旧版本没有 `workspace_id` 的记录会一次性迁移到 `sinopec-2027`。

未来创建空间只需要复用 `workspace-store.js` 的机制，不需要复制一套业务系统。

## 知识库分类

原始资料被压缩为可检索条目，并保留来源与可信度：

- 官方事实：考试流程、进面比例等明确规则。
- 历史数据：往届日期、岗位和竞争数据。
- 研究整理：题型、岗位能力、备考策略等归纳。
- 存在冲突：英语、题量、题型等来源不一致的信息。
- 推算：分数模型、竞争预测等只用于辅助决策。
- 系统规则：AI 必须区分事实、判断、推测和来源。

原始汇总文件标注的版本日期为 **2026-08-17**，晚于当前系统日期 **2026-08-16**；因此其中的“当前状态”按用户提供的待校验资料保存，不能替代后续官方公告。

## 隐私与同步

- API Key 只保存在当前设备，不进入工作空间数据、导出文件或 Gist。
- 学习内容、任务、笔记、知识库和复盘可以随工作空间同步。
- 归档只改变空间状态，不删除数据；归档空间默认不进入日常 AI 上下文。
- 手机端不再打开即裁剪历史学习记录，完整历史由本地和云端保留。
